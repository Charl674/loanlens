name: 'loanlens_dbt'
version: '1.0.0'
config-version: 2

profile: 'loanlens_dbt'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  loanlens_dbt:
    staging:
      +materialized: view
      +schema: STAGING
    marts:
      +materialized: table
      +schema: MARTS